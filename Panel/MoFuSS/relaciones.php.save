<?php
 include '../base.php';
                            $obj=new Base("localhost","root","global");

?>
                            <div class="form-group">
              <label for="name">Relacion: </label>
                            <?php
              $result = $obj->consulta("SELECT id,nombre,tipo from diagrama where pais=".$_POST["pais"]." order by tipo");
  $numfilas = $result->num_rows;
  if($numfilas>0){
    echo "<div id='operacion'>";
  for($x=0;$x<$numfilas;$x++){
  $fila = $result->fetch_object();
  echo "<button value='".$fila->id."' class='tipo".$fila->tipo."'>".$fila->nombre."</button>";

}
echo "<div id='diagrama' class='panelIzquierdo'></div>";
echo "</div>";
}
              ?>
          </div>
<script>
$("#operacion>button").on("click", function(){
if($("#diagrama>div").length==0){
var id=$(this).attr("value");
var tipo=$(this).attr("class");
var nombre=$(this).text();
$("#diagrama").append("<div id='"+id+"' name='"+tipo+"' class='elCont'><h1 class='"+tipo+"'>"+nombre+"</h1></div>");
}
else{}
});
</script>
